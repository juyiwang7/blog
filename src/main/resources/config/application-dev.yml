logging:
  config: classpath:config/logback-dev.xml
server:
  port: 8080

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/shenben?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name:  com.mysql.cj.jdbc.Driver
  web:
    loginurl: http://localhost:${server.port}/swagger-ui.html
    isopenurl: true
    googleexcute: explorer

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.hdu.blog.entity

